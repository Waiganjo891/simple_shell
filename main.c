#include "Functions.h"
/**
 * main - An integer
 * @ac: Another integer
 * @env: Another character
 * Return: Always (0)
 */
int main(int ac, char **env)
{
	if (ac == 1)
		prompt(env);
	return (0);
}
